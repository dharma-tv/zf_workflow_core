
server.servlet.context-path=/zanflow-api

# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=file:/home/ec2-user/zanflow-api/keystore.p12
# The password used to generate the certificate
server.ssl.key-store-password=zanflow
# The alias mapped to the certificate
server.ssl.key-alias=backend
server.ssl.enabled=true
server.port=8443

#server.port = 8081
## Spring DATASOURCE (local)
#spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
#spring.datasource.username= postgres
#spring.datasource.password=postgres
#spring.jpa.properties.hibernate.default_schema=sandbox


#User=zanflow, Schema=public, URL=jdbc:postgresql://zanflow.c9adasqkulpa.us-east-1.rds.amazonaws.com:5432/postgres
#passowrd=zanflow2020#
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.PostgreSQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = update

# App Properties
zanflow.app.jwtSecret= bezKoderSecretKey
zanflow.app.jwtExpirationMs= 86400000